import { Button } from "@kd/design-system";

export const Primary = () => <Button>Start a project</Button>;

export const Ghost = () => <Button variant="ghost">All 35 projects</Button>;

export const NavCta = () => <Button variant="nav">Contact</Button>;

export const AsLink = () => <Button href="#">View case study</Button>;

export const Disabled = () => <Button disabled>Start a project</Button>;
